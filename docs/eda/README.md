# EDA artifacts

Generated by:

```bash
python scripts/run_eda.py --dataset ml-latest-small --download
python scripts/run_eda.py --dataset ml-1m --download
```

Committed for **both** `ml-latest-small` and `ml-1m` (the comparison dataset): aggregate `*_stats.json` and PNG figures (rating distribution, user activity, item popularity, ratings over time).

**Not** committed: raw MovieLens files (see gitignored `data/`).
